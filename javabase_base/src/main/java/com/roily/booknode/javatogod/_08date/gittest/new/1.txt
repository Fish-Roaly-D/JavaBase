新建文件、确认提交

目的：提交，确认提交的东西、回滚待回滚的文件

第一种：提交本地、不push

可以undo  commit 撤销提交，对不需要提交的执行rollback  待修改完成在提交

新建的提交文件不可回到红色状态，已被git跟踪

可以undo commit  --->  rollback  在 提交


第二种：提交本地、push远程

1.txt 2.txt   new1  new2  1.txt

同样的undo commit, rollback  commit

第三种：后悔某次提交

基础版本